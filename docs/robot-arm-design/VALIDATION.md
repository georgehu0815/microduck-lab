# 第一阶段设计校验记录

日期：2026-09-12。范围：文档、静态网页和设计内部一致性；不是仿真/训练/实物验收。

## 已执行

- `python3 docs/robot-arm-design/validate_design.py`：通过。66 维单臂、116 维双臂切片连续且维度正确；六案例在文档和页面对应；本地链接有效；无伪造视频嵌入。
- 肩部假设重力矩复算为 `0.151392825 N·m`（显示精度取约数）；六电机堵转电流算术和 `8.82 A`。这里只验证算术，不证明连续力矩或实际供电能力。
- `git diff --check -- DESIGN.md docs/robot-arm-design`：通过；新增文件另由内容校验和网页检查覆盖。
- 网页执行者使用 Playwright 检查 1440 px 桌面和 390 px 移动布局，报告无横向溢出、无 console 错误；单/双臂按钮、六案例选择、鼠标与键盘导航通过。
- 设计负责人查看修订后的单臂概念图和移动端内容截图：机械臂为独立桌夹，Microduck 是控制节点，未显示直接机身挂载；案例指标明确标目标/未测。
- 独立审查发现并修正：机身挂载概念图误导、A3 网站/文档任务不一致、缺少障碍/航点/托盘内物品观测、A1 缺角度门槛、静态力矩门槛不能替代温升试验、scene/payload/morphology 身份混淆。

## Amazon 核验边界

用户搜索页及若干关键词搜索页返回 503；不能声明浏览了该页当前全部候选。RoArm-M2-S 的具体 Amazon 页面成功读取，Manufacturer Part Number 与 ASIN 匹配；厂商产品页和 wiki 用于复核电压、重量、控制接口与上电运动。其它搜索链接仅是检索入口，不保证有货或型号正确。没有购买操作。

## 原设计阶段未执行 / 不应推断

原设计阶段没有创建或运行新机械臂 MJCF、PPO、ONNX、真实视频或控制服务。后续软件实现及真实执行证据见 [IMPLEMENTATION.md](IMPLEMENTATION.md) 与 [RESULTS.md](RESULTS.md)，本节不是对新增实现的状态声明。实体装配、热试验和急停认证仍未完成；静态原型仍不是实际 `/arm` 页面。八案例训练器与机载 `robotd` 未因机械臂工作改变。

复核门槛见 `README.md` 和 `EXPERIMENTS.md`；后续阶段须用实际产物取代“待验证”状态。
